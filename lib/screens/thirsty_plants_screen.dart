import 'package:flutter/material.dart';

class ThirstyPlantsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Thirty'),
      ),
      body: Center(
        child: Text('Thirsty Page'),
      ),
    );
  }
}