import 'package:flutter/material.dart';

class PlantInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Plant info'),
      ),
      body: Center(
        child: Text('Plant Info Page'),
      ),
    );
  }
}